{
  "name": "discord-adapter",
  "description": "reads + sends Discord chat and mod commands. Needs a bot token + Server ID (enable the Message Content intent and invite the bot with Read/Send/Moderate perms).",
  "version": "0.1.0",
  "capabilities": "input",
  "root_file": "./src/main.rs",
  "launch_command": "cargo run",
  "command_flags": [
    "--release"
  ],
  "autostart": true,
  "credentials": [
    { "key": "bot_token", "label": "Bot Token (discord.com/developers)", "sensitive": true, "list": false },
    { "key": "guild_id", "label": "Server (Guild) ID", "sensitive": false, "list": false },
    { "key": "channels", "label": "Channel IDs to monitor (one per line; empty = all)", "sensitive": false, "list": true, "optional": true }
  ]
}